name: emoji_overview_app_example
description: "Emoji overview app example."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  emoji_extension:
    path: ../../
  flutter:
    sdk: flutter

dependency_overrides:
  collection: ^1.19.1

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

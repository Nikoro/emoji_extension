name: emoji_chat_app_example
description: "Emoji chat app example."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  emoji_extension:
    path: ../../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true